import Employee from '../lib/Employee';
const e = new Employee();

describe('Employee', () => {
    it('Should return Employee instance', () => {

        expect(typeof (e).toBe('object'))

    })
});
it('Should retun set name', () => {
    expect(e.name).toBe(name);

});
it


